<UserControl
    x:Class="Strack.Desktop.UI.Control.Chart.CardProcess"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Strack.Desktop.UI.Control.Chart"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="200"
    Height="80"
    mc:Ignorable="d">

    <Border
        Padding="10"
        Background="{DynamicResource MainWindow.Navigation.Item.Hover.Background}"
        CornerRadius="10">
        <DockPanel>

            <!--  进度条  -->
            <ProgressBar
                Margin="0,5,0,0"
                DockPanel.Dock="Bottom"
                Value="50" />

            <!--  进度信息  -->
            <DockPanel Margin="0,10,0,0" DockPanel.Dock="Bottom">
                <TextBlock
                    DockPanel.Dock="Right"
                    FontSize="20"
                    Text="70%" />
                <TextBlock FontSize="10" VerticalAlignment="Bottom" Text="65/80" />
            </DockPanel>

            <!--  顶部标题  -->
            <DockPanel>
                <TextBlock DockPanel.Dock="Right" Text="备注" />
                <TextBlock Text="标题" />
            </DockPanel>

        </DockPanel>
    </Border>

</UserControl>
